#include_directories(${PROJECT_SOURCE_DIR}/src/app/inc)

#include core libraries
#include_directories(${PROJECT_SOURCE_DIR}/src/core)
#link_directories(${PROJECT_SOURCE_DIR}/src/core)


#add_executable(Cecilion main.cpp)

# Add the engine library to the app
#target_link_libraries(Cecilion Engine)
